#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include "Conversion.h"

void TypeI(int n, char arg[],char s[]);
void TypeJ(int n, char arg[],char s[]);
void TypeR(int n, char arg[],char s[]);